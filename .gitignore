.DS_Store
/.build
/.staging
/Packages
xcuserdata/
DerivedData/
*.xcuserstate
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
*.dmg
# standalone CLI distribution tarball (produced by scripts/package.sh, shipped via release)
RockchipDDRTestUtilityCLI-macos.tar.gz

# Local dev tooling. Not part of the product; only the generated
# DDRTestFiles/<soc>/*.cfg are tracked.
/tools/

# Local design docs / specs / plans — dev-only, not shipped
/docs/
